import React from 'react';

const Header: React.FC = () => {
    return (
        <div style={{ display: "flex", width: "100%", height: "5%", background: "#242830", justifyContent: "right" }}>
        </div>
    );
};

export default Header;